# main file

# takes directory input of timetable files

# cleans the xlsx files to remove merge cells and deal with uncertain class

# then converts those xlsx files to csv

# then reads those csvs into pandas and cleans them into a DB-ready format

